def BI(arr):
    for x in range(arr[1], arr[len(arr) - 1], 1):
        sum+= arr[x-1]
        sum2 = 0
        for y in range (arr[x+1, arr[len(arr)-1], 1]):
            sum2 += arr[y]
        if sum == sum2
            return x
    return -1