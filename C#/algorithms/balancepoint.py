# balance point
def Bp(arr):
    for x in range(arr):
        sum += arr[x]
        sum 2 = 0
        for y in range(arr[x + 1], arr[len(arr) - 1], 1):
            sum2 += arr[y]
        if sum == sum2:
            retun True
    
    return False
